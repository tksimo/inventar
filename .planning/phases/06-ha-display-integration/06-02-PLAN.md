---
phase: 06
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - README.md
autonomous: false
requirements:
  - HA-02
  - HA-01
must_haves:
  truths:
    - "README.md contains a Home Assistant integration section with copy-pasteable YAML"
    - "README YAML includes an HA REST sensor configuration targeting http://homeassistant.local:8099/api/ha/summary"
    - "README YAML includes a Lovelace iframe card pointing at http://homeassistant.local:8099"
    - "README YAML sets scan_interval: 60 for the REST sensor (per RESEARCH Pitfall 5 guidance)"
    - "README YAML lists json_attributes: low_stock_items, out_of_stock_count, out_of_stock_items, total_items"
    - "User manually verifies that the iframe renders the Inventar app inside a live HA Lovelace dashboard"
  artifacts:
    - path: "README.md"
      provides: "HA integration docs (HA-01 sensor config + HA-02 iframe card)"
      contains: "/api/ha/summary"
  key_links:
    - from: "README.md"
      to: "backend/routers/ha_display.py"
      via: "documented resource URL"
      pattern: "http://homeassistant\\.local:8099/api/ha/summary"
    - from: "README.md"
      to: "config.yaml ports"
      via: "documented iframe URL using direct port 8099 (INFRA-05)"
      pattern: "http://homeassistant\\.local:8099"
---

<objective>
Document the HA integration so users can wire the sensor and iframe card without reading source code, then confirm the iframe renders in a live HA instance (HA-02 phase gate).

Purpose: HA-01 and HA-02 are only "done" once a user can follow the README and get working sensors + dashboard card. The backend work in Plan 01 produces the endpoint; this plan produces the documentation contract and the human-verified proof that iframe embedding actually works.

Output: README.md section with copy-pasteable YAML samples + a user-confirmed iframe render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ha-display-integration/06-CONTEXT.md
@.planning/phases/06-ha-display-integration/06-RESEARCH.md
@.planning/phases/06-ha-display-integration/06-01-PLAN.md
@README.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Home Assistant Integration" section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (read the full current file so the new section is inserted in a sensible location — after the "Architecture" section, before any "Development" / "Local build" sections; do not duplicate existing headings)
    - .planning/phases/06-ha-display-integration/06-CONTEXT.md (Specific Ideas block — the exact YAML the user confirmed)
    - .planning/phases/06-ha-display-integration/06-RESEARCH.md (Code Examples → "HA configuration.yaml sample" — includes scan_interval: 60 and total_items in json_attributes)
    - config.yaml (confirm ingress_port: 8099 and ports "8099/tcp" — the URL uses this port)
  </read_first>
  <action>
    Insert a new top-level section immediately after the existing "## Architecture" section in README.md (detect its end — first `^## ` line after it — and insert the new section before that next `## ` heading). The section must be titled exactly:

    `## Home Assistant Integration`

    Inside the section include, in this order:

    1. One short paragraph (2-3 sentences) explaining that the add-on exposes `/api/ha/summary` for REST sensors and that the app UI can be embedded as a Lovelace iframe using the direct port URL. Mention that both use `http://homeassistant.local:8099` (direct port — ingress is for the sidebar panel). Reference INFRA-05 for the direct-port guarantee.

    2. A subsection `### REST sensor (HA-01)` containing a YAML fenced code block exactly (keep indentation two-space, no tabs):

       ```yaml
       # configuration.yaml
       sensor:
         - platform: rest
           name: Inventar Low Stock
           resource: http://homeassistant.local:8099/api/ha/summary
           value_template: "{{ value_json.low_stock_count }}"
           scan_interval: 60
           json_attributes:
             - low_stock_items
             - out_of_stock_count
             - out_of_stock_items
             - total_items
       ```

       Follow the code block with a one-line note: "`scan_interval: 60` is recommended; the default 30s is unnecessarily frequent for inventory data."

    3. A subsection `### Lovelace iframe card (HA-02)` containing a YAML fenced code block exactly:

       ```yaml
       type: iframe
       url: http://homeassistant.local:8099
       title: Inventar
       ```

       Follow the code block with a one-line note: "The direct port URL is used (not the ingress panel URL) because Lovelace iframes cannot traverse HA ingress authentication."

    4. A subsection `### Response shape` showing an example JSON response:

       ```json
       {
         "low_stock_count": 3,
         "out_of_stock_count": 1,
         "total_items": 42,
         "low_stock_items": ["Coffee", "Milk", "Oat milk"],
         "out_of_stock_items": ["Pasta"]
       }
       ```

       With a one-line note: "`low_stock_items` lists items where status is LOW, or quantity is at/below the reorder threshold. `out_of_stock_items` lists items where status is OUT or quantity is 0. Archived items are excluded from all counts."

    Do not remove or reflow any existing content. Do not add emojis. Preserve line-width style of surrounding prose (~72 cols where possible — wrap prose but leave code blocks untouched).
  </action>
  <verify>
    <automated>grep -c "http://homeassistant.local:8099/api/ha/summary" README.md && grep -c "type: iframe" README.md && grep -c "## Home Assistant Integration" README.md</automated>
    Expected: each grep returns at least 1.
  </verify>
  <acceptance_criteria>
    - README.md contains the exact heading `## Home Assistant Integration`
    - README.md contains the exact subheadings `### REST sensor (HA-01)`, `### Lovelace iframe card (HA-02)`, `### Response shape`
    - README.md contains exact string `http://homeassistant.local:8099/api/ha/summary`
    - README.md contains exact string `platform: rest`
    - README.md contains exact string `scan_interval: 60`
    - README.md contains exact string `value_template: "{{ value_json.low_stock_count }}"`
    - README.md contains exact string `type: iframe`
    - README.md contains lines `- low_stock_items`, `- out_of_stock_count`, `- out_of_stock_items`, `- total_items` under json_attributes
    - README.md still contains all prior top-level headings (`## Phase 1 scope`, `## Architecture`, etc.) — use `grep -c "^## "` before vs after and confirm the count increased by exactly 1
    - No occurrences of placeholder text like `TODO`, `TBD`, or `FIXME` in the new section
  </acceptance_criteria>
  <done>README documents both HA-01 sensor config and HA-02 iframe card with copy-pasteable YAML that matches Plan 01's implemented endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify iframe renders in HA Lovelace (HA-02 phase gate)</name>
  <files>N/A — manual verification only; no files modified</files>
  <read_first>
    - README.md (the new "Home Assistant Integration" section just written — the user follows it verbatim)
    - .planning/phases/06-ha-display-integration/06-VALIDATION.md (Manual-Only Verifications table — this is the exercised procedure)
  </read_first>
  <action>
    Pause execution and present the verification procedure below to the user. Do NOT proceed past this checkpoint until the user replies with `approved` (case-insensitive). If the user reports any failure, capture the exact failure text in the plan summary, diagnose (e.g. iframe blocked → grep response headers for X-Frame-Options; sensor unavailable → curl the endpoint from HA host and check status), and propose fixes. Do not mark the checkpoint approved on the user's behalf.
  </action>
  <verify>
    <automated>MISSING — manual verification only; checkpoint satisfied by user "approved" reply per resume-signal below</automated>
  </verify>
  <what-built>
    - Backend: `GET /api/ha/summary` live on port 8099 (Plan 01)
    - CORS middleware allowing cross-origin GETs (Plan 01)
    - No X-Frame-Options / CSP frame-ancestors headers on responses (Plan 01, test-verified)
    - README section with a Lovelace iframe card snippet (this plan, Task 1)
  </what-built>
  <how-to-verify>
    1. Ensure the add-on is installed and running on the target HAOS instance (rebuild / reinstall if the backend code was updated).
    2. Confirm the backend endpoint responds from HA's host:
       - SSH into HAOS or use Terminal add-on: `curl -sS http://homeassistant.local:8099/api/ha/summary` — expect a JSON object with keys `low_stock_count`, `out_of_stock_count`, `total_items`, `low_stock_items`, `out_of_stock_items`.
    3. Add the REST sensor to `configuration.yaml` using the YAML from the new README section. Restart HA. In Developer Tools → States, locate `sensor.inventar_low_stock`; confirm state is a number and attributes include `low_stock_items`, `out_of_stock_items`, `total_items`.
    4. Open a Lovelace dashboard → Edit → Add Card → Manual → paste the iframe YAML from the README.
    5. Save the dashboard. Confirm the Inventar app UI renders inside the iframe with NO blocked-content error, NO "refused to connect" message, and NO blank frame.
    6. Click through one app route inside the iframe (e.g. open an inventory item) to confirm the SPA is functional inside the frame.
    7. Reload the dashboard — confirm the iframe still renders after a fresh load.
  </how-to-verify>
  <resume-signal>
    Type `approved` if the sensor state updates correctly AND the iframe renders the app with a working route. If any step fails, paste the exact error text (browser console, HA sensor state "unavailable", etc.) and we will diagnose.
  </resume-signal>
  <acceptance_criteria>
    - User response includes the literal string `approved` (case-insensitive)
    - If approved, both HA-01 and HA-02 phase success criteria from ROADMAP are met in a live HA environment
  </acceptance_criteria>
  <done>User confirms iframe renders with working route and sensor state is a live number in HA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries beyond Plan 01. This plan only changes documentation; the deployed attack surface is identical to Plan 01's.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | README exposes direct-port URL | accept | URL is already public via `config.yaml` ports_description; the documentation does not widen the attack surface. Data sensitivity covered under T-06-01. |
| T-06-07 | Spoofing | User copy-pastes wrong URL (e.g. ingress URL) into iframe card | accept | The README explicitly notes "The direct port URL is used (not the ingress panel URL)" — documentation mitigation sufficient. |
</threat_model>

<verification>
- `grep -c "## Home Assistant Integration" README.md` → 1
- `grep -c "/api/ha/summary" README.md` → at least 1
- `grep -c "type: iframe" README.md` → 1
- Human approval of iframe render in live HA Lovelace (Task 2)
</verification>

<success_criteria>
- README section provides complete, correct, copy-pasteable HA configuration for both the REST sensor and the iframe card.
- User has confirmed in a live HA environment that:
  - The REST sensor populates `sensor.inventar_low_stock` with a numeric state and the documented attributes.
  - The iframe Lovelace card renders the Inventar app without blocked-content errors.
- HA-01 and HA-02 phase goals fully satisfied end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ha-display-integration/06-02-SUMMARY.md` noting: README section added, user iframe verification outcome (quote the approval), any environmental quirks discovered during manual verify (e.g. scan_interval tuning, hostname resolution notes).
</output>
